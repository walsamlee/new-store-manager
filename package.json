{
  "name": "new-store-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "knex": "^0.15.2",
    "pg": "^7.6.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "node-mocks-http": "^1.7.3",
    "supertest": "^3.3.0"
  },
  "scripts": {
    "start": "node dist/app",
    "build": "babel --presets env server -d ./dist --ignore ./node_modules",
    "dev": "(babel --presets env server -d ./dist --ignore ./node_modules) && node dist/app",
    "test": "(babel --presets env server -d ./dist --ignore ./node_modules && dropdb --if-exists ueirllul && createdb ueirllul) && mocha dist/test/app.test.js --exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
